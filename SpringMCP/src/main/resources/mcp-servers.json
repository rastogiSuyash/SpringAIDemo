{
  "mcpServers": {
    "filesystem": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--mount", "type=bind,src=D:/mcp-project,dst=/projects",
        "mcp/filesystem",
        "/projects"
      ]
    }
  }
}